[Unit]
After=network.service

[Service]
ExecStart=/home/scripts/../start_daemon_userspaces.sh standalone
StandardOutput=syslog
StandardError=syslog
Restart=always
RestartSec=1min
ExecStop=/home/scripts/../start_daemon_userspaces.sh stop

[Install]
WantedBy=default.target
